#Definir as variaveis
soma = 0
num = int(input("Digite um número (digite 0 para parar): "))
# Quando 0 numero for diferente de 0 ele vai continuar somando.

while num != 0:
    num = int(input("Digite um número (digite 0 para parar): "))
    if num == 0:
        break
    soma += num

print("A soma dos números digitados é:", soma)
